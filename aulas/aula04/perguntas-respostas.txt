Utilizando a estrutura da pasta app do Next.js 13 para criar a página de adicionar nova memória e reaproveitar parte do design da página, assinale a alternativa abaixo que não corresponde a uma forma de realizar essa implementação:


Importar o arquivo layout.tsx em cada page.tsx que for utilizá-lo.

Escolha a alternativa incorreta na ação de logout implementada na aplicação:
*

Para remover um cookie, passamos a opção Delete-Cookie para o cabeçalho da resposta.

Qual a função dos middlewares?
*

Interceptar uma requisição.

Apesar do React Native e ReactJS atualmente compartilharem de muitas similaridades, ainda existem algumas diferenças. Assinale a alternativa abaixo que possui suporte na Web mas nenhum suporte no mobile:

HTML semântico.

Sobre a adição da funcionalidade upload e serviço estático de arquivos na aplicação Node.js, assinale a alternativa incorreta:

INCORRETA: Apesar de utilizarmos o formato multipart/form-data para o envio dos arquivos, também seria possível (e uma boa prática) enviar os arquivos diretamente via JSON.

CORRETA: 

É possível repassar configurações para os arquivos a serem aceitos pelo @fastify/multipart, como tamanho máximo.
Os arquivos podem ser servidos estaticamente pela rota /uploads/NOME_DO_ARQUIVO graças a configuração que fizemos com o @fastify/static
Apesar de utilizarmos o formato multipart/form-data para o envio dos arquivos, também seria possível (e uma boa prática) enviar os arquivos diretamente via JSON.